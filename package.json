{
  "name": "test",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "clean": "rimraf dist && rimraf ../pope/public/dist",
    "build": "parcel build",
    "copy": "cp -r dist ../pope/public",
    "start": "parcel"
  },
  "dependencies": {
    "axios": "^0.18.0",
    "sugar": "^2.0.4"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.1",
    "node-sass": "^4.7.2",
    "parcel-bundler": "^1.6.2",
    "rimraf": "^2.6.2"
  }
}
